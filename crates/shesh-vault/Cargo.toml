[package]
name = "shesh-vault"
version = "0.1.0"
edition = "2021"
description = "Command vault and parameter resolver for SheshAOS (Komandi integration)"
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
uuid.workspace = true
regex.workspace = true

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
