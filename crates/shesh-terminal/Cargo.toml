[package]
name = "shesh-terminal"
version = "0.1.0"
edition = "2021"
description = "PTY process manager and terminal emulator bridge for SheshAOS"
license = "MIT"

[dependencies]
shesh-kernel.workspace = true
portable-pty.workspace = true
tokio.workspace = true
thiserror.workspace = true
tracing.workspace = true

[dev-dependencies]
tempfile = "3"
